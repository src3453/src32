# sol (Stack Oriented Language) 仕様書

sol (Stack Oriented Language) は、スタック指向のプログラミング言語であり、SRC32アセンブリにコンパイルされる。
名称は小文字の "sol" で表記される。

スタックには32ビットの符号付き数値が入る。データスタックを持つ。 
文はスペースで区切られていて、左から解釈される。
ほとんどのオペレーターは短い (基本的に5文字以内) アルファベットで記述する。 
記号類は特殊な修飾子などに使われる。 
これらの文は最終的にSRC32のアセンブリにコンパイルされる。

# スタック
スタックは、LIFO (Last In First Out) のデータ構造であり、最後にプッシュされた値が最初にポップされる。
# 関数
関数は `fn name (arg1 arg2 ...) :` で始まり、関数名と引数名を `(...)` 内に指定する。関数の本体は `;` で終了する。

この仕様では、関数呼び出しのための呼び出し規約（calling convention）とスタックフレームのレイアウトを定める。コンパイラはこの規約に従ってコードを生成し、ランタイム（VM/アセンブリ）はそれを実行する。

重要な前提:
- データスタックは下方向（アドレスが小さくなる方向）に伸びるものとする（push は R28 を 4 バイト減算して書き込む）。R28 は実行時スタックポインタを指す。

## 呼び出し規約

solの関数呼び出しは、データスタックを引数受け渡しと式評価に使う。規約は次の通りとする。

1. 呼び出し側（caller）は、引数を左から右の順でデータスタックに積む。つまり `fn f (a b)` に対しては `a` を先に push し、その後に `b` を push する。
2. 呼び出し命令 `call @label` は、データスタック最上位から関数の引数個数ぶんを消費し、それらをその関数の引数として扱う。関数の下に残っている既存のスタック値は保持される。
3. 呼び出し時、ランタイムは関数用のフレームを確保し、引数とローカル変数はそのフレーム内の固定オフセットでアクセスされる。
4. 関数本体は、戻り値をデータスタックのトップに 1 個だけ残して `ret` を実行する。`ret` はフレームを解放し、呼び出し元へ戻る。
5. 戻り値を返さない関数は `retn` を使う。`retn` はフレームを解放し、戻り先に復帰するが、データスタック上の戻り値は残さない。

この規約では、関数呼び出しは「スタックの先頭に対する局所的な操作」であり、呼び出し前にスタック下位へ積まれていた値は関数終了後も保持される。したがって、関数呼び出しはスタック全体を丸ごと置き換えるのではなく、引数部分だけを消費して 0 個または 1 個の結果を返す。

## スタックフレームのレイアウト

フレームはコンパイル時に必要なローカル変数数（N_locals）と引数数（N_args）が分かればサイズが決定できる。

- フレームは 4 バイト単位で整列される。
- フレーム確保時の R28 の変化（スタックが下方向に成長するため）:
  - R28 := R28 - 4 * (N_locals + N_args)
- フレーム内のオフセット（R28 がフレームのベースを指す、すなわち最も低いアドレス）:
  - ローカル変数 i (0-based): address = R28 + 4 * i
  - local 0 は R28 + 0、local 1 は R28 + 4、... の位置に置かれる。
  - 引数 j (0-based, 左から右の順に arg0, arg1, ...): address = R28 + 4 * (N_locals + j)
  - arg0 はローカル領域の直後に置かれる。

この設計により、同じ関数内ではローカル変数と引数を固定オフセットでアクセスできる。ネストした呼び出しや再帰呼び出しの際でも、各呼び出しは独立したフレームを持つ。

## 変数参照と代入のコンパイル方針

- 変数名を式で使う（例: `a`）ときは、その変数の値をスタックにプッシュするコードが生成される。具体的には、フレーム内の対応するアドレスを計算して `ld` を行う。
- `>varname` のような代入は、スタックのトップの値を pop して変数のアドレスに `st` するコードにコンパイルされる。

例: 変数 `x` を読み出す場合、生成される擬似シーケンスは次のような形になる（アセンブリは実装依存）:
- (push address of x) ; 例: R28 + offset を計算してアドレスをスタックに置く
- `ld`                ; そのアドレスから値をロードして値をスタックに置く

代入 `>x` の生成:
- (push address of x)
- (push value)
- `st`                ; アドレスと値を使って書き込みを行う

## 呼び出し命令と戻り命令

- 呼び出し: `call @label`
  - 作用:
    1. 戻りアドレスを内部的に保存する。
    2. 関数が必要とする引数個数ぶんをデータスタック最上位から消費する。
    3. フレーム領域を確保し、引数とローカル変数のアクセス先を決める。
    4. ジャンプして関数本体を開始する。
- 戻り（戻り値あり）: `ret`
  - 作用:
    1. 関数は戻り値をデータスタックのトップに 1 個だけ残しておく。
    2. フレームを解放する。
    3. 保存していた戻り先へジャンプする。
    4. 呼び出し前にデータスタック下位にあった値は保持する。
- 戻り（戻り値なし）: `retn`
  - 作用:
    1. 関数は戻り値を返さない。
    2. フレームを解放してリターン先にジャンプする。
    3. 呼び出し前にデータスタック下位にあった値は保持し、callee が積んだ値は破棄する。

補足:
- 関数本体が明示的な `ret` または `retn` を含まずに終了した場合、コンパイラは関数の終端に暗黙の `retn` を挿入する（戻り値なしでの復帰を行う）。
- 再帰呼び出しやネストされた呼び出しに対応するため、フレームサイズは呼び出しごとに独立であること。

例:
```
fn add_two_numbers (a b) :
    local sum
    a b add >sum   # sum = a + b
    sum            # 戻り値として sum をスタックトップに残す
;

# 呼び出し側
1 2 add_two_numbers
prn  # 出力: 3
```

# スタックへの値のプッシュ
スタックに値をプッシュするには、数値を直接書く。
例:
```
1 2 3
```

# オペレーターの使用
オペレーターはスペースで区切られた文として記述される。各オペレーターは指定された数のオペランドをスタックから取り出し、結果をスタックに置く。
例:
```
1 2 add  # 1と2を加算し、結果の3をスタックに置く
3 2 sub 2 add  # 3から2を減算し、結果の1をスタックに置き、次に1と2を加算し、結果の3をスタックに置く
```

# ラベル
コードには `@` で始まるラベルを付けることができる。ラベルはジャンプ命令のターゲットとして使われる。ラベルは一意でなければならない。
ラベル名に `-`, `_` 以外の特殊文字を含むことはできない。(Regex: `[a-zA-Z_][a-zA-Z0-9_]*`)

# コメント
コメントは `#` で始まり、行の終わりまで続く。

# 数値リテラル
10進数の整数リテラルは、符号付き32ビット整数として解釈される。負の数は `-` 記号で表される。
例:
```
42 -17 0
```
符号なし整数リテラルは, `u` 接尾辞を付けて表される。符号なし整数は、0から2^32-1までの範囲である。
例:
```
42u 17u 0u
```

16進数の整数リテラルは、`0x` 接頭辞を付けて表される。16進数の整数は、0から2^32-1までの範囲である。
例:
```
0x2A 0x11 0x0
```

# 文字列リテラル
文字列リテラルは、二重引用符で囲まれたテキストである。文字列リテラルは、文字列の先頭文字のアドレスをスタックに置く。文字列リテラルは、ヌル文字で終端される。
例:
```
"Hello, World!"  # 文字列リテラル (スタック内のアドレスは文字列の先頭('H')を指す)
prn  # スタックのトップのアドレスを取得し、ヌル文字までの文字列を出力する
```
`prn` オペレーターなど、文字列を扱うオペレーターは、文字列の先頭アドレスをスタックから取得し、ヌル文字までの文字列を使用する。

# 文
文 (statements) は、スペースで区切られたオペレーターとオペランドの組み合わせで構成される。それぞれのオペレーターとオペランドを単語 (words) と呼ぶ。

# 文の区切り
単語はスペースで区切られる。文の終わりは、改行またはセミコロン `;` で示される。タブ、２文字目以降の連続したスペースなどは無視される。
例:
```
a     b c ;  # 文の終わりはセミコロンで示される
```

# 値の型
solでは、すべての値は32ビットの符号付き整数として扱われる。符号なし整数は、`u` 接尾辞を付けることで表される。文字列は、ヌル文字で終端された文字列へのアドレスとして扱われる。

# オペレーター
## 1. 算術演算
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `add` | 2 | a b -> a+b | スタックのトップ2つの値を加算し、結果をスタックに置く |
| `sub` | 2 | a b -> a-b | スタックのトップ2つの値を減算し、結果をスタックに置く |
| `mul` | 2 | a b -> a*b | スタックのトップ2つの値を乗算し、結果をスタックに置く |
| `div` | 2 | a b -> a/b | スタックのトップ2つの値を除算し、結果をスタックに置く |
| `mod` | 2 | a b -> a%b | スタックのトップ2つの値を除算し、余りをスタックに置く |
| `neg` | 1 | a -> -a | スタックのトップの値を符号反転し、結果をスタックに置く |
## 2. ビット演算
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `and` | 2 | a b -> a&b | スタックのトップ2つの値のビットごとのANDを計算し、結果をスタックに置く |
| `or` | 2 | a b -> a|b | スタックのトップ2つの値のビットごとのORを計算し、結果をスタックに置く |
| `xor` | 2 | a b -> a^b | スタックのトップ2つの値のビットごとのXORを計算し、結果をスタックに置く |
| `not` | 1 | a -> ~a | スタックのトップの値のビットごとのNOTを計算し、結果をスタックに置く |
| `shl` | 2 | a b -> a<<b | スタックのトップの値を2つ目の値だけ左にシフトし、結果をスタックに置く |
| `shr` | 2 | a b -> a>>b | スタックのトップの値を2つ目の値だけ右にシフトし、結果をスタックに置く |
## 3. スタック操作
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `dup` | 1 | a -> a a | スタックのトップの値を複製し、スタックに置く |
| `drop` | 1 | a -> | スタックのトップの値を破棄する |
| `swap` | 2 | a b -> b a | スタックのトップ2つの値を入れ替える |
| `over` | 2 | a b -> a b a | スタックの2つ目の値を複製し、スタックに置く |
| `rot` | 3 | a b c -> b c a | スタックのトップ3つの値を回転させる (1つ目の値が3つ目に、2つ目の値が1つ目に、3つ目の値が2つ目に移動する) |
| `nip` | 2 | a b -> b | スタックの2つ目の値を破棄し、トップの値を残す |
| `tuck` | 2 | a b -> b a b | スタックのトップの値を2つ目の値の下に挿入する |
## 4. 比較演算
比較演算の結果は真偽値として表される。真は `0`、偽は `1` とする。
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `eq` | 2 | a b -> a==b | スタックのトップ2つの値が等しいかどうかを比較し、結果をスタックに置く (0: 等しい, 1: 等しくない) |
| `neq` | 2 | a b -> a!=b | スタックのトップ2つの値が等しくないかどうかを比較し、結果をスタックに置く (0: 等しくない, 1: 等しい) |
| `lt` | 2 | a b -> a<b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値より小さいかどうかを比較し、結果をスタックに置く (0: 小さい, 1: 小さくない) |
| `gt` | 2 | a b -> a>b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値より大きいかどうかを比較し、結果をスタックに置く (0: 大きい, 1: 大きくない) |
| `le` | 2 | a b -> a<=b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値以下かどうかを比較し、結果をスタックに置く (0: 以下, 1: 以下でない) |
| `ge` | 2 | a b -> a>=b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値以上かどうかを比較し、結果をスタックに置く (0: 以上, 1: 以上でない) |
| `sgn` | 1 | a -> a>>31&1 | スタックのトップの値の符号ビットを取り出し、結果をスタックに置く (1: 負, 0: 正 (0を含む)) |
## 5. ロード/ストア
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `ld` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから32ビットの値をロードし、スタックに置く |
| `st` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の32ビットの値をストアする |
| `ldb` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから8ビットの値をロードし、スタックに置く |
| `stb` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の8ビットの値をストアする |
| `ldh` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから16ビットの値をロードし、スタックに置く |
| `sth` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の16ビットの値をストアする |
## 6. 制御フロー
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `jmp @label` | 0 | -- | ラベルに無条件ジャンプする |
| `jz @label` | 1 | a -> | スタックのトップの値をpopし、それが0の場合、ラベルにジャンプする |
| `jnz @label` | 1 | a -> | スタックのトップの値をpopし、それが0でない場合、ラベルにジャンプする |
## 99. 特殊 
TODO: 番号を変更する
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `stacksize` | 0 | -- | スタックの最大サイズをスタックに置く |
| `halt` | 0 | -- | プログラムの実行を終了する |

# コンパイラ・ディレクティブ
ディレクティブの先頭には`!`が付く。ディレクティブは、コンパイラに対して特定の指示を与えるために使用される。
| ディレクティブ | 説明 |
| --- | --- |
| `!include "filename"` | 指定されたファイルをインクルードする |
| `!define MACRO_NAME value` | マクロを定義する |
| `!undef MACRO_NAME` | マクロを未定義にする |
| `!const CONST_NAME value` | 定数を定義する |
| `!var VAR_NAME [value]` | 変数を定義する。初期値は省略可能で、省略時は 0 で初期化される。|
| `!end` | 現在のディレクティブのブロックを終了する |
| `!required_stack_size size` | スタックの必要サイズを指定する（実装のスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する） |
| `!force_stack_size size` | スタックの強制サイズを指定する (ランタイムは使用可能なスタックサイズを強制的にこれに設定する; 実装のスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する) |
| `!asm` | `!end`までの以降の行をアセンブリコードとして扱う |
| `!data` | `!end`までの以降の行をデータセクションとして扱う |
| `!db` | `!end`までの以降の行をバイトデータとして扱う |

## `!asm`
`!asm`ディレクティブは、以降の行をアセンブリコードとして扱うことを指定する。`!end`ディレクティブが現れるまで、すべての行はアセンブリコードとして解釈される。
コンパイラは、`!asm`ディレクティブの後に続く行をそのままアセンブリコードとして出力する。これにより、特定のアセンブリ命令を直接使用することができる。
例:
```
!asm
; ここからアセンブリコード
LDI R1, 0x1234 ; Load VDP Mode Register Address
LDI R2, 0x01 ; Load value to set VDP to text mode
STB [R1], R2 ; Set VDP to text mode
!end
```

## `!data`, `!db`
`!data`ディレクティブは、以降の行をデータセクションとして扱うことを指定する。`!end`ディレクティブが現れるまで、すべての行はデータとして解釈される。
`!db`ディレクティブは、以降の行をバイトデータとして扱うことを指定する。`!end`ディレクティブが現れるまで、すべての行はバイトデータとして解釈される。
データを記述するには、数値リテラルをスペース区切りで並べる。数値リテラルは10進数、16進数（0xプレフィックス）、または2進数（0bプレフィックス）で表すことができる。
`!data`では、各数値リテラルは4バイトのデータとして扱われる。それ以下の数値はパディングされ、4バイトに拡張される。上位ビットは符号拡張される。
`!db`では、各数値リテラルは1バイトのデータとして扱われる。上位ビットは無視され、下位8ビットのみが使用される。
例:
```
!data
# ここからデータセクション
0x12345678 -12345 # 4バイトのデータ
!end

!db
# ここからバイトデータセクション
0x01 0x02 0xFF 0x00 # 1バイトのデータ
!end
```

# コンパイラオプション

- グローバル変数の割当ベースアドレスを指定するにはコンパイラの `--var-base` オプションを使用する（デフォルト: 0x00100000）。
- 実行時スタック(R28)の初期位置を指定するにはコンパイラの `--stack-top` オプションを使用する（デフォルト: 0x000FFFFC）。

## solc のレジスタスタックキャッシュ

`solc` の SRC32 コード生成では、データスタックのトップ付近を `R1`〜`R12` にキャッシュできる。  
この最適化は実装上の都合であり、sol 言語のスタック意味論（演算結果やスタック効果）は従来と同一である。

- キャッシュ対象レジスタ: `R1`〜`R12`（12要素）
- 管理方式: リングバッファ（top を head として管理）
- キャッシュに空きがある push はメモリアクセスなしでレジスタに積む
- キャッシュが満杯の push は、最下位（最古）の 1 要素のみをメモリへ spill してから新しい top を積む
- キャッシュ要素が不足した参照（例: `dup`/`swap`/二項演算入力）は、必要なぶんだけメモリから fill して復元する

### spill / fill と `R28`

- spill: `R28 := R28 - 4` の後に `ST` し、キャッシュ外へ退避する
- fill: `LD [R28 + 0]` 後に `R28 := R28 + 4` し、キャッシュへ復元する
- したがって `R28` は「キャッシュの直下にあるメモリ上スタック先頭」を指す

### 制御フロー境界の整合ルール

実装の単純性と安全性を優先し、次を採用する。

- `call` / `jmp` / `jz` / `jnz` / `halt` の直前でキャッシュを flush する
- 分岐先ラベル到達時は「空キャッシュ状態」から再開する
- これにより、制御フロー分岐を跨いだときもメモリ上スタック表現が正準となる

### 関数呼び出し時の規約

- `R1`〜`R12` は caller-saved として扱い、`call` 境界では保存/復元せず flush で整合を取る
- 関数本体の `ret` / `retn` でも flush を行った上で既存のフレーム復帰規約に従う


# 定数・変数
定数は、`!const` ディレクティブを使用して定義される。定数は、プログラムの実行中に変更することはできない。
変数は、`!var` ディレクティブを使用して定義される。変数は、プログラムの実行中に変更することができる。
変数への値の格納は、`>` 接頭辞を使用して行う。
例:
```
!const PI 3.14159
!var num 3
PI prn
num PI mul >num  # numにPIを掛けた値を格納
num prn  # 出力: 9.42477
```

# ローカル変数
ローカル変数は、関数の本体内で定義され、関数の呼び出しが終了すると破棄される。ローカル変数は、関数の引数として渡される値を格納するために使用される。
ローカル変数は、関数の本体内で `local` キーワードを使用して宣言される。`local var_name initial_value` の形式で、初期値を指定することもできる。初期値が指定されない場合は、ローカル変数は`0`で初期化される。
ローカル変数は、関数の本体内でのみ有効であり、関数の外部からアクセスすることはできない。
ローカル変数への値の格納は、`>` 接頭辞を使用して行う。
例:
```
fn example_function (a b) :
    local c  # ローカル変数cを定義
    a b add >c  # aとbを加算し、結果をローカル変数cに格納
    c 2 mul  # ローカル変数cの値を2倍する
;
1 2 example_function prn  # 出力: 6
```

# 変数スコープ

名前解決の優先順位:

1. ローカル変数
2. 関数引数
3. グローバル変数
4. 定数
5. 未定義エラー

同一スコープ内で同名変数を定義することは禁止する。
例:
```
fn example_function (a b) :
    local a  # エラー: 同一スコープ内で同名変数を定義することは禁止されている
    a b add
;
```    

# コンパイラ、ツールチェイン
solは`solc`というコンパイラを使用して、SRC32のアセンブリにコンパイルされる。`solc`は、ソースコードを解析し、構文エラーや型エラーを検出する。エラーが検出された場合、コンパイルは中止され、エラーメッセージが表示される。

# 標準ライブラリ
solには、標準ライブラリ(`solstd`)が含まれており、基本的な入出力操作や文字列操作、数学関数などが提供される。標準ライブラリは、`!include` ディレクティブを使用してインクルードすることができる。
TODO: 標準ライブラリの仕様を追加する

## 1. 出力 (solstd_print.sol)
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `prn` | 1 | a -> | スタックのトップの値を文字列へのポインタとして、標準出力に出力する |
| `prnln` | 1 | a -> | スタックのトップの値をポインタとして、標準出力に出力し、改行する |
| `putc` | 1 | a -> | スタックのトップの値を文字として標準出力に出力する |