# sol (Stack Oriented Language) 仕様書
スタックには32ビットの符号付き(リターンスタックは符号なし)数値が入る。データスタックとリターンスタックを持つ。 
文はスペースで区切られていて、左から解釈される。
ほとんどのオペレーターは短い (基本的に5文字以内) アルファベットで記述する。 
記号類は特殊な修飾子などに使われる。 
これらの文は最終的にSRC32のアセンブリにコンパイルされる。

# スタック
スタックは、LIFO (Last In First Out) のデータ構造であり、最後にプッシュされた値が最初にポップされる。スタックは、データスタックとリターンスタックの2つの部分からなる。
- データスタック: プログラムの実行中に値を格納する
- リターンスタック: サブルーチンの呼び出しと戻りのために使用される
スタックのサイズは実装に依存する。スタックオーバーフロー、スタックアンダーフローは、エラーとして扱われ、プログラムは停止する。
solには、スタックの最大サイズを取得する特別なオペレーターがある。`stacksize` は、スタックの最大サイズを返す。

# スタックへの値のプッシュ
スタックに値をプッシュするには、数値を直接書く。
例:
```
1 2 3
```

# オペレーターの使用
オペレーターはスペースで区切られた文として記述される。各オペレーターは指定された数のオペランドをスタックから取り出し、結果をスタックに置く。
例:
```
1 2 add  # 1と2を加算し、結果の3をスタックに置く
3 2 sub 2 add  # 3から2を減算し、結果の1をスタックに置き、次に1と2を加算し、結果の3をスタックに置く
```

# ラベル
コードには `@` で始まるラベルを付けることができる。ラベルはジャンプ命令のターゲットとして使われる。ラベルは一意でなければならない。
ラベル名に `-`, `_` 以外の特殊文字を含むことはできない。(Regex: `[a-zA-Z_][a-zA-Z0-9_]*`)

# コメント
コメントは `#` で始まり、行の終わりまで続く。

# 数値リテラル
10進数の整数リテラルは、符号付き32ビット整数として解釈される。負の数は `-` 記号で表される。
例:
```
42 -17 0
```
符号なし整数リテラルは, `u` 接尾辞を付けて表される。符号なし整数は、0から2^32-1までの範囲である。
例:
```
42u 17u 0u
```

16進数の整数リテラルは、`0x` 接頭辞を付けて表される。16進数の整数は、0から2^32-1までの範囲である。
例:
```
0x2A 0x11 0x0
```

# 文字列リテラル
文字列リテラルは、二重引用符で囲まれたテキストである。文字列リテラルは、文字列の先頭文字のアドレスをスタックに置く。文字列リテラルは、ヌル文字で終端される。
例:
```
"Hello, World!"  # 文字列リテラル (スタック内のアドレスは文字列の先頭('H')を指す)
prn  # スタックのトップのアドレスを取得し、ヌル文字までの文字列を出力する
```
`prn` オペレーターなど、文字列を扱うオペレーターは、文字列の先頭アドレスをスタックから取得し、ヌル文字までの文字列を使用する。

# 文
文 (statements) は、スペースで区切られたオペレーターとオペランドの組み合わせで構成される。それぞれのオペレーターとオペランドを単語 (words) と呼ぶ。

# 文の区切り
単語はスペースで区切られる。文の終わりは、改行またはセミコロン `;` で示される。タブ、２文字目以降の連続したスペースなどは無視される。
例:
```
a     b c ;  # 文の終わりはセミコロンで示される
```

# 値の型
solでは、すべての値は32ビットの符号付き整数として扱われる。符号なし整数は、`u` 接尾辞を付けることで表される。文字列は、ヌル文字で終端された文字列へのアドレスとして扱われる。

# オペレーター
## 1. 算術演算
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `add` | 2 | a b -> a+b | スタックのトップ2つの値を加算し、結果をスタックに置く |
| `sub` | 2 | a b -> a-b | スタックのトップ2つの値を減算し、結果をスタックに置く |
| `mul` | 2 | a b -> a*b | スタックのトップ2つの値を乗算し、結果をスタックに置く |
| `div` | 2 | a b -> a/b | スタックのトップ2つの値を除算し、結果をスタックに置く |
| `mod` | 2 | a b -> a%b | スタックのトップ2つの値を除算し、余りをスタックに置く |
| `neg` | 1 | a -> -a | スタックのトップの値を符号反転し、結果をスタックに置く |
## 2. ビット演算
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `and` | 2 | a b -> a&b | スタックのトップ2つの値のビットごとのANDを計算し、結果をスタックに置く |
| `or` | 2 | a b -> a|b | スタックのトップ2つの値のビットごとのORを計算し、結果をスタックに置く |
| `xor` | 2 | a b -> a^b | スタックのトップ2つの値のビットごとのXORを計算し、結果をスタックに置く |
| `not` | 1 | a -> ~a | スタックのトップの値のビットごとのNOTを計算し、結果をスタックに置く |
| `shl` | 2 | a b -> a<<b | スタックのトップの値を2つ目の値だけ左にシフトし、結果をスタックに置く |
| `shr` | 2 | a b -> a>>b | スタックのトップの値を2つ目の値だけ右にシフトし、結果をスタックに置く |
## 3. スタック操作
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `dup` | 1 | a -> a a | スタックのトップの値を複製し、スタックに置く |
| `drop` | 1 | a -> | スタックのトップの値を破棄する |
| `swap` | 2 | a b -> b a | スタックのトップ2つの値を入れ替える |
| `over` | 2 | a b -> a b a | スタックの2つ目の値を複製し、スタックに置く |
| `rot` | 3 | a b c -> b c a | スタックのトップ3つの値を回転させる (1つ目の値が3つ目に、2つ目の値が1つ目に、3つ目の値が2つ目に移動する) |
| `nip` | 2 | a b -> b | スタックの2つ目の値を破棄し、トップの値を残す |
| `tuck` | 2 | a b -> b a b | スタックのトップの値を2つ目の値の下に挿入する |
## 4. 比較演算
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `eq` | 2 | a b -> a==b | スタックのトップ2つの値が等しいかどうかを比較し、結果をスタックに置く (1: 等しい, 0: 等しくない) |
| `neq` | 2 | a b -> a!=b | スタックのトップ2つの値が等しくないかどうかを比較し、結果をスタックに置く (1: 等しくない, 0: 等しい) |
| `lt` | 2 | a b -> a<b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値より小さいかどうかを比較し、結果をスタックに置く (1: 小さい, 0: 小さくない) |
| `gt` | 2 | a b -> a>b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値より大きいかどうかを比較し、結果をスタックに置く (1: 大きい, 0: 大きくない) |
| `le` | 2 | a b -> a<=b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値以下かどうかを比較し、結果をスタックに置く (1: 以下, 0: 以下でない) |
| `ge` | 2 | a b -> a>=b | スタックのトップ2つの値を比較し、1つ目の値が2つ目の値以上かどうかを比較し、結果をスタックに置く (1: 以上, 0: 以上でない) |
| `sgn` | 1 | a -> (a<0) | スタックのトップの値の符号ビットを取り出し、結果をスタックに置く (0: 正 (0を含む), 1: 負) |
## 5. ロード/ストア
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `ld` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから32ビットの値をロードし、スタックに置く |
| `st` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の32ビットの値をストアする |
| `ldb` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから8ビットの値をロードし、スタックに置く |
| `stb` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の8ビットの値をストアする |
| `ldh` | 1 | a -> a | スタックのトップの値をアドレスとして、そこから16ビットの値をロードし、スタックに置く |
| `sth` | 2 | a b -> | スタックのトップの値をアドレスとして、そこへ2番目の16ビットの値をストアする |
## 6. リターンスタック操作
* a: データスタックのトップの値, A: リターンスタックのトップの値
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `rpush` | 1 | a -> A | スタックのトップの値をリターンスタックにプッシュする |
| `rpop` | 0 | A -> a | リターンスタックのトップの値をポップし、スタックに置く |
| `rpeek` | 0 | A (消費されない) -> a | リターンスタックのトップの値を覗き見し、スタックに置く |
## 7. 制御フロー
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `jmp @label` | 0 | -- | ラベルに無条件ジャンプする |
| `jz @label` | 1 | a -> | スタックのトップの値をpopし、それが0の場合、ラベルにジャンプする |
| `jnz @label` | 1 | a -> | スタックのトップの値をpopし、それが0でない場合、ラベルにジャンプする |
## 99. 特殊 
TODO: 番号を変更する
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `stacksize` | 0 | -- | スタックの最大サイズをスタックに置く |
| `retsize` | 0 | -- | リターンスタックの最大サイズをスタックに置く |
| `halt` | 0 | -- | プログラムの実行を終了する |

# コンパイラ・ディレクティブ
ディレクティブの先頭には`!`が付く。ディレクティブは、コンパイラに対して特定の指示を与えるために使用される。
| ディレクティブ | 説明 |
| --- | --- |
| `!include "filename"` | 指定されたファイルをインクルードする |
| `!define MACRO_NAME value` | マクロを定義する |
| `!undef MACRO_NAME` | マクロを未定義にする |
| `!const CONST_NAME value` | 定数を定義する |
| `!var VAR_NAME [value]` | 変数を定義する。初期値は省略可能で、省略時は 0 で初期化される。|
| `!end` | 現在のディレクティブのブロックを終了する |
| `!required_stack_size size` | スタックの必要サイズを指定する（実装のスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する） |
| `!required_return_stack_size size` | リターンスタックの必要サイズを指定する（実装のリターンスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する） |
| `!force_stack_size size` | スタックの強制サイズを指定する (ランタイムは使用可能なスタックサイズを強制的にこれに設定する; 実装のスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する) |
| `!force_return_stack_size size` | リターンスタックの強制サイズを指定する（ランタイムは使用可能なリターンスタックサイズを強制的にこれに設定する; 実装のリターンスタックサイズが指定されたサイズより小さい場合、プログラムはランタイムエラーで終了する） |
| `!asm` | `!end`までの以降の行をアセンブリコードとして扱う |

## `!asm`
`!asm`ディレクティブは、以降の行をアセンブリコードとして扱うことを指定する。`!end`ディレクティブが現れるまで、すべての行はアセンブリコードとして解釈される。
コンパイラは、`!asm`ディレクティブの後に続く行をそのままアセンブリコードとして出力する。これにより、特定のアセンブリ命令を直接使用することができる。
例:
```
!asm
; ここからアセンブリコード
LDI R1, 0x1234 ; Load VDP Mode Register Address
LDI R2, 0x01 ; Load value to set VDP to text mode
STB [R1], R2 ; Set VDP to text mode
!end
```

# コンパイラオプション

- グローバル変数の割当ベースアドレスを指定するにはコンパイラの `--var-base` オプションを使用する（デフォルト: 0x00100000）。
- 実行時スタック(R28)の初期位置を指定するにはコンパイラの `--stack-top` オプションを使用する（デフォルト: 0x0000FFFC）。


# 関数
関数は `fn (args) :` で始まり、関数名と引数の数を`(arg1 arg2 ...)`で指定する。関数の本体は、`;` で終了する。
関数の終了時に、スタックのトップに残っている値が関数の戻り値となる。
関数は、呼び出されるときに指定された引数の数だけスタックから値を取り出し、関数の本体で使用する。
`()` 内には関数が受け取る引数名を指定する。引数は左から右に順に割り当てられる。
引数は呼び出し時にデータスタックから取得され、関数内ではローカル変数として扱われる。
関数終了時、データスタックのトップに存在する値が戻り値となる。
戻り値が不要な場合、関数終了前に `drop` で破棄する。
関数を途中で終了させたい場合は、明示的に `ret` を使用する。`ret` は、関数の実行を終了し、呼び出し元に戻る。
例:
```
fn add_two_numbers (a b) :
    a b add
;

1 2 add_two_numbers prn  # 出力: 3
```

# 定数・変数
定数は、`!const` ディレクティブを使用して定義される。定数は、プログラムの実行中に変更することはできない。
変数は、`!var` ディレクティブを使用して定義される。変数は、プログラムの実行中に変更することができる。
変数への値の格納は、`>` 接頭辞を使用して行う。
例:
```
!const PI 3.14159
!var num 3
PI prn
num PI mul >num  # numにPIを掛けた値を格納
num prn  # 出力: 9.42477
```

# ローカル変数
ローカル変数は、関数の本体内で定義され、関数の呼び出しが終了すると破棄される。ローカル変数は、関数の引数として渡される値を格納するために使用される。
ローカル変数は、関数の本体内で `local` キーワードを使用して宣言される。`local var_name initial_value` の形式で、初期値を指定することもできる。初期値が指定されない場合は、ローカル変数は`0`で初期化される。
ローカル変数は、関数の本体内でのみ有効であり、関数の外部からアクセスすることはできない。
ローカル変数への値の格納は、`>` 接頭辞を使用して行う。
例:
```
fn example_function (a b) :
    local c  # ローカル変数cを定義
    a b add >c  # aとbを加算し、結果をローカル変数cに格納
    c 2 mul  # ローカル変数cの値を2倍する
;
1 2 example_function prn  # 出力: 6
```

# 変数スコープ

名前解決の優先順位:

1. ローカル変数
2. 関数引数
3. グローバル変数
4. 定数
5. 未定義エラー

同一スコープ内で同名変数を定義することは禁止する。
例:
```
fn example_function (a b) :
    local a  # エラー: 同一スコープ内で同名変数を定義することは禁止されている
    a b add
;
```    

# コンパイラ、ツールチェイン
solは`solc`というコンパイラを使用して、SRC32のアセンブリにコンパイルされる。`solc`は、ソースコードを解析し、構文エラーや型エラーを検出する。エラーが検出された場合、コンパイルは中止され、エラーメッセージが表示される。

# 標準ライブラリ
solには、標準ライブラリ(`solstd`)が含まれており、基本的な入出力操作や文字列操作、数学関数などが提供される。標準ライブラリは、`!include` ディレクティブを使用してインクルードすることができる。
TODO: 標準ライブラリの仕様を追加する

## 1. 文字列操作 (solstd_string.sol)
| オペレーター | オペランド数 | スタック操作 | 説明 |
| --- | --- | --- | --- |
| `prn` | 1 | a -> | スタックのトップの値を標準出力に出力する |
| `prnln` | 1 | a -> | スタックのトップの値を標準出力に出力し、改行する |
| `strlen` | 1 | a -> len(a) | スタックのトップの値を文字列の先頭アドレスとして、ヌル文字までの長さを計算し、スタックに置く |
| `tostr` | 1 | num -> a | スタックのトップの数値を文字列に変換する |
| `tohex` | 1 | num -> a | スタックのトップの数値を16進数文字列に変換する |
| `tobin` | 1 | num -> a | スタックのトップの数値を2進数文字列に変換する |
| `putc` | 1 | a -> | スタックのトップの値を文字として標準出力に出力する |
